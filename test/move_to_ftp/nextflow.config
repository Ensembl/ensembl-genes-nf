params {
    // FTP destination - override this when running the test
    ftp_destination = '/nfs/ftp/public/databases/ensembl/riboseq/internal'

    // Output directory
    outdir = 'results'
}

process {
    executor = 'slurm'
    time = '1.h'
    memory = '5.GB'
}
