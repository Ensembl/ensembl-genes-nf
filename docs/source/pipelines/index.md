# Pipelines

Each pipeline page describes the user-facing purpose, expected inputs, main stages, output layout, and parameters. The parameter reference pages are generated from the corresponding `nextflow_schema.json` files.

```{toctree}
:maxdepth: 2

example
repeat
riboseq
translon-consensus
```
